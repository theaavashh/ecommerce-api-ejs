<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="font-serif flex justify-center">
    <div class="container mt-8">
      <h1 class="text-3xl ml-64">Order Details</h1>
      <% if (locals.msg){%>
        <div
          class="bg-purple-100 border border-purple-400 text-black tex-2xl text-center px-4 mt-5 py-3 rounded relative"
          role="alert"
        >
          <strong class="font-semibold text-2xl font-serif"><%= msg %></strong>
        </div>
        <%}%>
      <form class="flex flex-col mt-5 ml-72 justify-between" action="order" method="POST">
        <h3 class="text-2xl">Contact Information</h3>
        <div class="form-label flex flex-col ml-5 text-xl">
          <label>Full Name</label>
          <input type="text" class="border border-2 border-black h-9 w-96" name="fullName" value="<%= fullname %>"/>
        </div>
        <div class="form-label flex flex-col ml-5 mt-3 text-xl">
          <label>Contact Details</label>
          <input type="text" class="border border-2 border-black h-9 w-96" name="contact" value="<%= contact %>"/>
        </div>
        <div>
          <h3 class="text-2xl mt-5">Shipping Information</h3>
          <div class="form-label flex flex-col ml-5 text-xl">
            <label>Shipping Address</label>
            <input type="text" class="border border-2 border-black h-9 w-96" name="shippingAddress" />
          </div>
        </div>
        <div>
            <h3 class="text-2xl mt-3">Payment Details</h3>
            <div class="form-label flex flex-col ml-5 text-xl">
              <label>Payment Mode</label>
              <input type="text" class="border border-2 border-black h-9 w-96" name="paymentMode" />
            </div>
            <div class="form-label flex flex-col m-5 text-xl">
                <label>Sub Total</label>
                <div><%= total %></div>
                <input type="hidden" class="border border-2 border-black h-9 w-96" value="<%= total %>" name="totalprice"/>
              </div>
          </div>
          <button class="text-lg w-36 p-2 bg-red-200 ml-24">Make Payment</button>
        </div>

        <div class="mt-16">
          <h3 class="text-2xl ml-8">Product Information</h3>
          <div class="ml-[-200px]">
            <% data.forEach((element)=>{%>
            <div
              class="flex justify-around items-center w-[600px] ml-[200px] mt-10 text-2xl bg-white h-26"
            >
              <img src="/temp/<%=element.avatar%>" width="100" height="100" />
              <div class="text-2xl" name="productDetails">
                Product: <%= element.productName.toUpperCase() %>
              </div>
              <input type="hidden" name="productDetails" value="<%= element.productName%>"/>
            </div>
            <%})%>
          </div>
        </div>

      </form>
    </div>
  </body>
</html>
